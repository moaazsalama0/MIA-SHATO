# FastAPI + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Kokoro TTS
kokoro>=0.9.2

# ML core (CPU-only to avoid CUDA downloads)
torch==2.2.2+cpu
torchaudio==2.2.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Audio + processing
soundfile
numpy==1.26.4
pydantic>=2.7,<3
# Logging is built-in to Python (no extra package)
